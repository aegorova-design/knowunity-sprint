# Component gaps

Things a screen needed that Storybook did not have. Each line says what it was
and which screen needed it. Built inline in the screen, from tokens.

A thing that appears here twice gets built properly as a component with a
story instead — see `.claude/skills/build-screen/SKILL.md`, step 5.

- The record button's visible label and helper ("Tap to start" / "About 30 seconds is plenty") — `/explain/[term]` (06 Idle) and, as "Tap to stop", `/explain/[term]/recording` (07 Recording). `recordButton`'s own `label` is its accessible name, not visible text, and the copy is centred under it. **Second screen to need it, so by the rule above it is due to be built once** — the whole mic zone is, in fact: both screens centre the same column on the same Space/300 gap and Space/200 lead-in, and `recordingScreen.css` currently repeats `idleScreen.css` rule for rule. Deferred rather than done, because it means editing a screen that is already signed off; 07 needs an extra slot above the button for the status line and the waveform, which 06 has nothing in.
- `href` on `buttonIcon` and `recordButton` — `/explain/[term]` (06 Idle). `button` renders a Next `Link` when given one; these two take `onClick` only, so the screen wraps them in a client shim to push the route.
- The session app bar — a close `buttonIcon`, a `progressIndicator` and a Skip `button` in one `appBar` — `/explain/[term]` (06 Idle) and `/explain/[term]/hint`. Storybook has `appBar` and its three slots, but nothing that fills them the way SPEC.md's screen-detail preamble prescribes for every session screen. Second screen to need it, so by the rule above it is built once at `src/app/explain/[term]/SessionAppBar.tsx` instead of copied a third time. No story: it is a composition of library components that only means anything inside the session, so it sits with the screens rather than in `src/components`.
- The term prompt — `MascotFigure size="S"` beside a `TextBlock variant="M"` carrying the question and the same ask — `/explain/[term]` (06 Idle) and `/explain/[term]/type` (15 + 16 Text fallback). SPEC.md lists "the prompt" as a component of both. Second screen to need it, so by the rule above it is built once at `src/app/explain/[term]/TermPrompt.tsx`. No story, for the same reason as the session app bar: it is a composition that only means anything inside the session.
- A helper line under a field ("Typed answers are judged the same way and count the same.") — `/explain/[term]/type` (15 + 16 Text fallback). `textField` carries no helper of its own, and `textBlock` would make it a heading. Built as a `<p>` in caption type, tied to the field with `aria-describedby`. Adjacent to the record button's helper above but not the same thing — that one is a centred two-line label-and-helper under a button, this one is a single left-aligned line owned by an input. If a third helper turns up, the three together are a component.
- Knowie peeking over a centred speech bubble — `/explain/intro` (04 First run, mic primer). Not `mascotMessage`: that sits her at size S beside a left-aligned bubble, and this frame centres her at size L over a full-width bubble with centred copy. Matching it would mean forking the component, so the bubble is a `<p>` in the screen and the overlap is a negative margin on her.
- **The outlined card — and it is now two screens, so it is due.** A bordered box at `radius.600` on a `stroke.border` hairline in `border.default`, holding a list on a Space/400 rhythm. `/explain/intro` (04 First run, mic primer) draws the "how it works" card — three `iconSlot` + text lines as a `<ul>`, because the three lines are parallel statements rather than a paragraph. `/explain/denied/how` draws the numbered steps — an `<ol>` with the marker carrying the count, ordered rather than parallel, which is why it is not the primer's icon list. **The list inside differs; the card around it does not** — `introScreen.css` and `howSheet.css` set the same four rules, and the second was written by copying the first so the two would read as one family. Nothing in Storybook draws a card. Logged as two separate entries until now, which is how a straight duplicate stayed invisible: by the rule at the top of this file, the card is the thing to build once and the two lists stay with their screens.
  - One thing to carry over when it is built: the icon's colour sits on a wrapper span, not on `iconSlot`. The component spreads its props after its own `className`, so passing one would replace `knowieIconSlot` rather than add to it. The same is true of `textBlock` and every other component that spreads `...rest` last.
- The bottom sheet's panel — the rounded surface with a grabber — `/explain/intro/example` (05 First run, example) and `/explain/denied/how`. `scaffold` positions the `bottomSheetOnly` slot and draws the scrim, but nothing draws the panel. Second screen to need it, so by the rule above it is built once at `src/app/explain/SheetPanel.tsx` rather than twice inline. No story, and it stays with the screens rather than in `src/components`, for the same reason `SessionAppBar` does: design-system.md does not let a screen invent a design-system component.
- ~~**An alignment prop on `textBlock`.**~~ **Resolved by decision.** Both sheet titles are centred in Figma, and `textBlock` fixes variant M to the left. Rather than add a prop or fork the component, the design owner moved both titles to **variant S** — a label over the sheet's content rather than a screen heading — which ranges left by the component's own rule. The title colour stays `text/primary`; `textBlock` has no colour prop and did not gain one. Nothing to build here.
- ~~`TermPrompt`'s shape used outside the session.~~ **Resolved by widening the prop, exactly as this entry predicted.** `/explain/intro/example` (05 First run, example) draws Knowie beside a question, which is what `TermPrompt` draws — but "guild" is not one of the session's three terms and the frame hides the caption, so a component keyed to a `Term` could not express it and the sheet kept its own copy of the rule. `TermPrompt` now takes **`prompt`** with optional `caption`, `showCaption` and `titleAs`, rather than a `Term`; the session's four screens pass `term.prompt` and take the defaults, and the sheet passes its own question with the caption off at `h3`. `.exampleSheet-prompt` is gone. Still no story, and it stays in `[term]/`: widening a prop did not make it a design-system component.
- **The plan screen's tabs** — `/plan` and `/plan/in-progress`. Two equal halves, Plan and Materials, with the active one carried by weight and colour and no underline, which is how Figma draws it. **Storybook has no tabs component**, so `planScreen.css` sets the row and both labels' type itself and `PlanScreen.tsx` marks the active one with a `data-active` attribute. They are `<span>`s rather than buttons or links on purpose: Materials is out of scope for the sprint, so the pair reports where the student is rather than offering a second destination — which also means nothing here is focusable and there is no `tablist` to make accessible. If Materials ever becomes real, this stops being a picture of tabs and needs both a component and the ARIA that goes with it.
- A second `href` shim for `buttonIcon`, at `src/app/explain/navigation.tsx` — `/explain/intro` (04) and `/explain/denied` (14). Extends the `href` gap logged above. Deliberately not `CloseSessionButton` from `[term]/navigation.tsx`: that one leaves a session in progress, and these two screens are before a session exists.
- **Focus is not trapped inside a sheet.** `SheetPanel` is a `section` with an accessible name, not a `dialog`, and carries no `aria-modal` — claiming one without trapping focus would be a lie to a screen reader. The screens behind (05, `/explain/denied/how`) are marked `inert` instead, which keeps Tab out of the covered controls. A real focus trap is the thing still missing.
- Tap-outside-to-dismiss on a sheet — `/explain/intro/example` (05) and `/explain/denied/how`. `scaffold` draws the scrim from `showBottomSheetBackground` but exposes no handler for it, and the slot it gives is only as tall as the panel. `SheetPanel` therefore carries its own layer: a `Link` anchored to the slot's bottom at `100dvh`, painting under the panel, pointing at the route underneath. No component change, no client component, and Back still works. It is `aria-hidden` and out of the tab order — the accessible way out stays the sheet's own button, so assistive tech never meets a full-screen unlabelled link.
- **A helper-line component is now due.** The existing helper gap above says "if a third helper turns up, the three together are a component". It has: `introScreen-primer` on 04 ("Next, your browser asks for the mic…") is a centred caption line above a button, which is the record button's helper in all but position, and the text screen's helper is the same step ranged left. Three occurrences of one idea, in three files. Building it means touching 06 Idle and the text fallback, so it is logged rather than done — a call for the design owner. `howSheet-intro` is deliberately **not** counted: it is body copy at the Body S step, not a caption under a control.
- The recording status line — "Listening, 0:11", a live counter — `/explain/[term]/recording` (07 Recording). Nothing in Storybook carries a running time. Built inline as a `<p>` in Body S Bold on `accent.brand.bold`, the one place `waveform.css` allows brand violet to mean live audio. The word "Listening" sits beside it so the colour never carries the state alone.
- **`waveform` packs its bars to the left in any container wider than they are.** The component is `width: 100%`, but its 24 bars are fixed-width on a fixed gap and total 234px, with no `justify-content` — so in the 358px content column the row draws hard against the left edge. 07 Recording sizes a wrapper to the row's own content width to bring it back under the mic zone's centring. Every future consumer will hit this: the fix belongs in `waveform.css`, not in each screen.
- ~~**`waveform` has no motion, and SPEC.md assumes it does.**~~ **Resolved.** `Live` now runs a staggered listen loop: each bar scales from `--waveform-listen-depth` (0.55) to its resting height on `motion.duration.listen` (1200ms) with `motion.easing.listen`, lagging its neighbour by `motion.duration.listenStagger` (120ms) — about 2.4 crests travelling the row, which is the design owner's call over the calmer single swell. Delays are negative so the row starts mid-figure rather than assembling itself. `prefers-reduced-motion: reduce` is a single `animation: none`, because the crest is the resting figure and the frozen frame was already correct. Specified against a scratch motion preview, signed off on the numbers, and pinned by a story.
- **The thumb zone's action stack, built as `src/app/explain/ActionStack.tsx`.** A Primary with an optional supporting row or button under it at Space/200 — `04 First run, mic primer`, the hint screen, `14 Permission denied` and the text fallback had each built the same column in their own CSS. Deliberately **not** `buttonGroup`: its Vertical variant is the same idea, but its documented contract is a Primary over a *Secondary* at the group's own size, and none of the four fit — the text fallback puts a Tertiary M under a Primary L, and the other three put a *pair* there. The design owner's call was not to stretch a documented contract for screens it was not drawn for: `buttonGroup` keeps `17` and `18 Summary`, which are what it was drawn for, and the sprint carries its own stack. No story, and it stays with the screens rather than in `src/components`, for the same reason `SheetPanel` and `SessionAppBar` do. Every new screen with a thumb zone uses it.
- **The row of ways out, built as `src/app/explain/ButtonPair.tsx`.** Secondary M buttons sharing the width in equal halves at Space/200 — `06 Idle`, the hint screen, `04` and `14` had four byte-identical copies of the rule. Deliberately **not** `buttonGroup`'s Horizontal variant, which is a different shape: that one pairs a `buttonIcon` hugging its square with a Primary that fills, so it carries one label; this carries two at equal width. It stands alone where there is no Primary to sit under (`06 Idle`, where the mic is the main action), and a single child fills the row rather than taking half — the `05b` and `09c` case, measured at 358 against the pair's 175 + 8 + 175. Mockups v2 screen `10b` draws this row at **16**; that frame is the odd one out against every other screen in the flow, and the design owner's call is that **8** (Space/200) is correct everywhere, `10b` included. Extended since: `10` and `10b` draw their whole vertical thumb stack at 16 too, and the same call applies there — see `VerdictActions`.
- ~~**A headline-m step on `textBlock`.**~~ **Resolved by decision — nothing to build.** The plan screen's subject heading is set in `--greed-headline-m-*`, and `textBlock`'s four steps are display-m / headline-xl / headline-s / body-s-bold, so the component cannot carry it. Rather than add a fifth step for one heading, the design owner left `textBlock` alone and the heading stays inline in `planScreen.css`. Revisit only if a second screen needs the same step — then it is a real gap in the scale, not one screen's exception.
- ~~**`takePlayer` has no playback position, and no way to drive one.**~~ **Resolved.** `takePlayer` and `waveform` both take `played` (0 to 1) now — the take's real position, fed by the screen's clock. `waveform` rounds it to the nearest **bar**, so a take fills in 24 steps rather than the four quarters `progress` can name, and the fill keeps travelling however long the take is instead of sitting at the halfway mark. Rounding to the nearest lights the last bar just before the end, so playback finishes on a full row. `Playing` and `Default` both draw a position — which is what lets a **pause hold its place** instead of returning to the start, the second half of this gap — and `Silent` ignores it. With `played` left off, every state fills exactly as its Figma variant does, so nothing that already used the component changed. **The press tokens are reused, not duplicated:** each bar's flip from unplayed to played runs `motion.duration.pressOut` (140ms) on `motion.easing.press`, on the same grounds `skeleton` borrows `motion.easing.listen` — a bar settling into a new state is what that curve already describes, and a second near-identical pair under a new name would be tokens earning nothing. Signed off as a reuse; if a playback flip ever wants its own number, that is a token decision, not a screen's. Reduced motion drops the flip and keeps the fill advancing, because position is what SPEC.md's motion rule keeps running. Pinned by a story on each component. **Two things still need the design owner:** `waveform`'s Figma description still says a take reads in quarters and not continuously, which is no longer true, and neither `played` nor `onPlayPause` has a Figma property to live in. `18 Summary, term tapped` gets all of it for free when it is built.
- **`verdictHeader` has no way to hide its caption.** `/` (01 Home, first session). The frame leads with Knowie and the exam line alone and switches the caption layer off on its instance — something an instance can do and a component cannot express, since the set has no `showCaption` boolean the way `textBlock` does. Rendering the caption empty was the alternative and is worse: an empty paragraph still takes the stack's Space/300 gap, so the headline sits off-centre against a hole. **Built as a code-only `showCaption` prop**, defaulting to true so every screen already using the header is untouched, and pinned by a story. The component's Figma description now names the missing property as a gap, which is the half that still needs the design owner: a boolean on the set would make the frame and the code say the same thing, and `20 Home, revisit` — which shows its caption — is the frame that proves both states are real.
- **A centred column of hugging actions.** `/home/revisit` (20 Home, revisit). The frame stacks a Primary over a Tertiary on Space/200, both hugging their labels and centred under the header — 166 and 145 wide against the 358 column. `ActionStack` is the same stack at the same gap but stretches its children to fill, which is right in the explain flow's thumb zone and wrong here, and it lives with the explain screens rather than with home. Built as four rules in `home.css` (`.home-actions`) rather than loosening `ActionStack` into two shapes. **Second screen with a centred action column and the first outside the session**, so if a third wants it, the honest fix is a `stretch` or `align` choice on `ActionStack` and a move out of `src/app/explain`.
- **The sheet header's outcome badge, and now its outcome word too.** `/explain/summary/[term]` (18 Summary, term tapped). The sheet leads with the same trio `termRow` carries — the outcome's badge, the term, the outcome's word — and design-system.md is explicit that it "shows the same trio but is not this component". `termRow`'s `BADGE_ICON` map and its four badge colours are private to it, so the sheet repeats both from tokens rather than reaching into it. The updated header then replaced `statusTag` with the outcome as a coloured word at Body S Bold, so the screen now sets the badge **and** the word from the same four colours `statusTag` and `termRow` carry — three copies of one palette, in two files that have to be changed together. **Second place the same four-colour badge is drawn**, so the honest fix is a component of its own — `outcomeBadge`, or a `statusTag` variant that is a word rather than a pill — that `termRow` and this header both nest.
- **A label over a block inside a sheet.** `/explain/summary/[term]` (18 Summary, term tapped). "Your last attempt", Caption M Bold on text/secondary, sitting over the take. `textBlock`'s S step is Body S Bold on text/primary and is what the sheet's own title uses, so a second one at that step would read as a second title. Built as a token-set `h3` in `termSheet.css`.
- ~~**`chips` needs a name even where the frame draws no label.**~~ **Resolved by the component.** `18 Summary, term tapped` drew the four key ideas straight under the answer with nothing naming them, so `CoveredIdeas` gained `hideLabel` — a visible line taken away, an accessible name kept. The updated `answerBlock` then took the whole group into its Answer variant, name and all, and `13`, `13b` and `18` now hand it `keyIdeas` rather than drawing a group of their own. `hideLabel` had no consumer left and is gone with it. `CoveredIdeas` stays for `10` and `10b`, which draw "You covered" and no answer block.
- ~~**A second screen needs `08 Review`'s take player.**~~ **Resolved.** `ReviewTake` was the review screen's own client component; `18 Summary, term tapped` plays a take too, so it moved to `src/app/explain/TakePlayback.tsx` and took a `surface` prop — Page on `08`, Sheet inside the summary's sheet, which is `takePlayer`'s own rule for a player in `bottomSheetOnly`. Nothing about the playback changed with the move.
- **Knowie does not move, and SPEC.md's motion rule assumes she does.** `/explain/[term]/checking` (09 Processing). The rule names "Knowie's roll" among the things `prefers-reduced-motion` stops, and `skeleton`'s own story argues the block stays still precisely because "Knowie is the moving part" — but `mascotFigure` has no motion, and no duration or easing token in `tokens/tokens.json` describes a mascot idle loop. Picking one here would be inventing a token, which design-system.md forbids, so the roll is **not built**: the screen stays alive on the stepping status line, the tap pulse and — since the shimmer landed — `skeleton` itself. Knowie is now the one still thing on a screen whose premise is that she is the moving one, which is a reason to close this rather than a reason it no longer matters. It needs a number from the design owner, the way `motion.duration.listen` got one, and then it belongs in `mascotFigure` rather than in this screen.
- **`verdictHeader` gives no way to move Knowie on her own.** `/explain/[term]/checking` (09 Processing). The tap acknowledgement SPEC.md asks for — "a tap is inert but Knowie acknowledges it with a pulse" — has to scale the whole header, title and caption with her, because `mascotFigure` is nested inside the component and reaching past its root to style it would be a fork. At Space/0 padding and a 1.06 beat the drift is small, but the pulse is wider than the spec's word for it. If Knowie ever needs to move alone on a verdict screen too, the motion belongs on `mascotFigure`.
- **`chips` is always a `<button>`, and the verdict screens need a marker.** `/explain/[term]/pass` (10), `/explain/[term]/pass-hinted` (10b), `/explain/[term]/answer` (13) and `/explain/[term]/answer/said-back` (13b). SPEC.md prescribes the component by name and by prop — `Chips size="S" active="True" showLeftIcon={false} showRightIcon={false}` — but on this screen the four chips are a *record* of what the answer covered, not a set of options: the term is already resolved and there is nothing to select. The component renders a `<button type="button">` with `aria-pressed` whatever it is handed, so the screen ships four focusable toggle buttons that do nothing and announce as "pressed". `disabled` would fix the tab stop but is not in the component's docs or in any story, and design-system.md does not let a screen reach past a documented prop, so it is **not** used. Built exactly as SPEC.md prescribes and logged instead. Both `CoveredIdeas` and, since the update, `answerBlock`'s own Answer variant wrap the row in a named `<ul>` so the four at least announce as one set, but they are still four buttons — and on 13 they are four *unpressed* toggles, which is worse: a screen reader now offers to press each of the four ideas the student has just been told. **It is not a fix a screen can make** — a read-only mode belongs in the component, as a `Static` variant or an `as` that renders a `<span>`. The row now sits inside `answerBlock` on `13`, `13b` and `18 Summary, term tapped`, and in `CoveredIdeas` on `10` and `10b`, so one decision would settle five screens through two places instead of five. A decision for the design owner.
- ~~The "You covered" label over its chips.~~ **Resolved by building it once, as `src/app/explain/CoveredIdeas.tsx`.** The label over the key-idea chips, Greed/Caption M Bold on `text/secondary`, centred, at Space/300 — `10 Got it` and `10b Got it, after a hint` draw it identically, so the second screen to need it stopped copying it. `textBlock` cannot carry it: its smallest step is Body S Bold and it has no alignment prop, so the label is an `<h2>` set from tokens, with the chips as a `<ul>` under it. The label is a prop, not a constant, because `13` and `13b` draw the same chips under "The key ideas". No story, and it stays with the screens rather than in `src/components`, for the same reason `ActionStack`, `ButtonPair`, `SheetPanel` and `SessionAppBar` do. Still adjacent to the helper-line gap above — that one is a caption under a *control*, this is a caption over a *group* — and if a fourth caption-grade line turns up, the type step itself wants a home.
- ~~The outcome line under the Primary.~~ **Resolved by building the whole thumb stack once, as `src/app/explain/VerdictActions.tsx`.** A verdict screen's thumb zone: the one Primary, an optional supporting button, and the outcome line that says what the term recorded — `+15 XP · unaided`, `+10 XP · hinted`. `10 Got it` and `10b` both draw it, so the second screen to need it stopped copying it. ~~Deliberately **not** `ActionStack`.~~ **It is now built on `ActionStack`** — the buttons were the same Primary-with-something-under-it every other thumb zone draws, so keeping a second copy of that column bought nothing but a second place for the gap to drift. What is left here is the caption. The old argument for staying apart — that the last row is a caption rather than an action — turned out to be the argument for the *caption* sitting outside the stack, not for the buttons sitting outside it. The buttons keep **Space/200** among themselves — the thumb zone's gap across the whole flow, now read once from `actionStack.css` instead of being set again here. Both verdict frames draw Space/400 between the buttons, and the design owner's call is that they are the odd ones out, the same ruling `buttonPair.css` already records against `10b`. The **caption** sits at **Space/400** below the stack, per `10` and `13`: it is not an action, so it takes the wider step this flow puts between a button stack and the content beside it — the same step `08 Review` puts above its player. Until this change it sat at Space/200 and read as a third row of the stack. `12b`, `13` and `13b` draw the same line and should use this. The XP table still lives in `script.ts` rather than here — `UNAIDED_XP` and `hintedXp` — because what a term is worth is the mocked recall's business, not the thumb zone's.
- **The verdict screens' centred column, shared as `src/app/explain/verdictBody.css`.** `10` and `10b` both centre a Space/600 column of verdict header over covered ideas, and both frames write that centring as a fixed spacer above and below — 120 on `10`, 88 on `10b`, because `10b`'s thumb zone is a row taller. A stylesheet rather than a composition: there is nothing in it but a box and two rules, and a component to carry two children would be a layer for its own sake. `12b`, `13` and `13b` read down the same column.
- **`10b`'s acknowledged state is not drawn anywhere.** `/explain/[term]/pass-hinted` (10b Got it, after a hint). SPEC.md gives 10b one state and one line about what happens after: "Say it back → `/explain/[term]/recording` and back to this screen acknowledged." There is no frame for acknowledged and no copy for it. Built by following `13b Answer revealed, said back`, the one place the file does draw the same beat: the Say it back button goes, and the caption names what the student just did — "You said it back in your own words." The verdict, the chips and the outcome line are untouched, because the term was already a Pass and SPEC.md's XP table pays nothing for a say-back. **The caption is written here, not read off a frame** — a design-owner call.
- **A say-it-back take has nowhere of its own to go.** `/explain/[term]/pass-hinted` (10b). SPEC.md sends the student to `07 Recording` and says they come back acknowledged, but the recording screen's only exits are `08 Review` and Idle, and a say-back is not judged — there is no verdict to change and no XP to win, so sending it through `09 Processing` would be a 2.5s wait pretending to decide something already decided. `07` therefore takes an optional `?back=` — the screen to return the take to — which also disables Skip and advances the bar, since the term is resolved while it is set. Only a path inside the same term is honoured, because the value arrives in a URL. Review is skipped on this path: it exists to let a student check a take *before sending*, and nothing is being sent. If saying it back should let the student hear it first, that is a change to this decision, not a bug in it.
- ~~The hint screen's card stack.~~ **Resolved by building it once, as `.verdictCards` in `src/app/explain/verdictBody.css`.** The passages a verdict hands back, stacked at Space/300 and filling the content width — Figma's `cards` layer. `11`, `12` and `12b` all draw it, so it stopped being copied at the second. Nothing in Storybook stacks blocks, and `answerBlock`'s own "All three kinds" story stacks them in a demo wrapper rather than a component. A shared rule rather than a composition, and it sits beside `.verdictBody` because they are the same concern: the verdict screens' middle column. One child is a supported case, not a degenerate one — that is `12b`.
- ~~Each screen joining its own query onto a link.~~ **Resolved by building it once, as `src/app/explain/href.ts`.** The session has no store: everything a screen knows about the run travels in the URL, so links now carry more than one thing — a take's length and the rung of the ladder it was made on, or where a say-it-back returns to — and the second cannot assume it is the first. `RecordingTake`, `TypeScreen` and `08 Review` had each grown their own `?`/`&` join. `withQuery(href, params)` is that join, with undefined values dropped so an optional can be passed straight in. Not a component and not in Storybook: it draws nothing.
- **The hint ladder's transcripts and rungs are written, not drawn, for terms 2 and 3.** `/explain/[term]/hint-1` (11). Every Mockups v2 frame that draws a take draws Feudalism's, and Feudalism is term 1 — the term the script never sends down the ladder. So term 1's two takes and two hints are the frames' copy verbatim, and **Serfdom's and Manorialism's are written in `session.ts`**, pitched to miss one key idea at a time so that hint 2 has something left to say. Serfdom is the term the scripted run actually walks, so its copy is what a demo shows. A design-owner call, flagged rather than buried.
- ~~**Frames 11 and 12 have each other's captions.**~~ **Resolved by decision — un-swapped, and one word cut.** `/explain/[term]/hint-1` (11) and `/explain/[term]/hint-2` (12). Frame 11 carries "You have three of the four key ideas." over a take about people voting for local leaders; frame 12 carries "That's a different idea. Here's a bigger nudge." over a take that is nearly right. Every other layer on each frame agrees with itself — the title (`Not quite` / `Almost there`), the take, the hint label — so the caption is the layer that moved. The screens un-swap them: **11 reads "That's a different idea. Here's a nudge." and 12 reads "You have three of the four key ideas."** The extra word is cut because "a **bigger** nudge" is what that line would say on the second rung, and on the first there is nothing for it to be bigger than. Two divergences from the frames' copy, both deliberate. **The frames themselves still need fixing.**
- **Frame 12b quotes frame 12's take, unchanged.** `/explain/[term]/last-miss` (12b Not quite, last attempt). 12b is reached after a *third* take, and the frame shows the second one's words verbatim — a student who has just spoken again would see their previous answer sitting there unchanged, which reads as a bug rather than as a transcript. `session.ts` therefore carries three takes per term, and 12b quotes `heard[2]`. Term 1's first two takes are the frames' copy; **every third take, and both of Serfdom's and Manorialism's, are written**, pitched to keep missing the idea the spent hint was pointing at. A design-owner call, flagged rather than buried.
- **Frame 12b draws Skip live; SPEC.md says disabled.** `/explain/[term]/last-miss` (12b). SPEC.md's Skip rule names 12b among the disabled screens, and its "Rules that must hold on every screen" repeats it, so the screen disables Skip and the frame loses. Worth noting because the frame is not obviously wrong on its own terms — the term has not resolved yet, and the progress bar in that same frame correctly has not advanced. It is the one screen where Skip is off while the term is still unresolved, and the reason is that the ladder, not the term, is what ran out.
- **The frames do not advance the progress bar on the reveal; SPEC.md does.** `/explain/[term]/answer` (13 Answer revealed). Measured across Mockups v2, the bar is drawn advanced on `10` and `10b` (66 of 268) and unadvanced everywhere else, `13` and `13b` included (20 of 268) — so the frames say a pass moves the bar and a reveal does not. SPEC.md's Progress rule says it "advances a third on **any** resolution, including a skip", and its Skip rule groups 13 and 13b with the resolved screens, and the summary counts the revealed term. The bar's value is behaviour rather than visual detail, so SPEC.md wins and 13 advances. The rule the screens now follow is clean: the bar moves exactly where the outcome is recorded — `10`, `10b`, `13`, `13b` — and not on `12b`, where the ladder has run out but the term has not yet resolved. **The frames need fixing, or the rule needs an exception written down.**
- **The answers themselves are written for Serfdom and Manorialism.** `/explain/[term]/answer` (13). Only Feudalism's answer exists in Figma, and Feudalism is the term the script never reveals — Serfdom is. Both written answers name each of their four key ideas in a clause of its own, which is what the frame's Feudalism answer does, so the chips underneath have something to point at. A design-owner call, flagged rather than buried.
- **The reveal's caption is counted, and SPEC.md only asked for a condition.** `/explain/[term]/answer` (13). SPEC.md asks for "a caption that credits the attempt only if there was one", and the frame writes the two-take case: "You tried twice, and that's what makes this stick now." A binary would have printed "twice" at a student who tried once or three times — and the scripted run reaches this screen after **three** takes, so the frame's own number is not the one a demo shows. The screen therefore takes `?tries=`, carried by every link in, and swaps the number: once / twice / three times. **The no-take caption is written** — "Read it through, then say it back in your own words." — because with nothing to credit the line's next best job is pointing at the Primary. Both the extra numbers and the no-take line are copy decisions.
- ~~An unticked mode on the key-ideas group.~~ **Resolved by a prop on `CoveredIdeas`.** `13` lists the four ideas the *answer* contains, with `Chips active="False"`, where `10` and `10b` tick the four the *student* covered. Same label-over-chips group otherwise, so `active` joined `label` and `ideas` rather than a second composition being built. `13b` ticks them again once the student has said them back.
- ~~The reveal's middle column.~~ **Resolved by building it once, as `src/app/explain/[term]/RevealedAnswer.tsx`.** Knowie over the answer block over the four key ideas — `13 Answer revealed` and `13b Answer revealed, said back` draw it identically apart from the title, the caption and whether the ideas are ticked, so it stopped being copied at the second screen. `verdict="Neutral"` is held inside rather than exposed: both screens are Neutral for a reason worth keeping still — being shown an answer is not a wrong answer, and saying it back afterwards is not a right one — and a prop would invite a third reveal that quietly reads as a miss. It sits in `[term]/` beside `TermPrompt` rather than in `src/components`, for the reason design-system.md gives.
- **Next term on `13` is Secondary, where SPEC.md and the frame both say Tertiary.** `/explain/[term]/answer` (13). The design owner's call. The intent SPEC.md describes is unchanged — moving on is allowed but not encouraged, and Say it back keeps the Primary — but Tertiary put the screen's one way forward at the lowest emphasis in the whole flow, below even the pairs of ways out on the hint screens before it. `13b` still promotes it to Primary, and no other screen changes. **The frame still draws Tertiary.**
- **`verdictBody.css` is now read by a screen that is not a verdict.** `05b Resume` centres Knowie over a `textBlock` in a Space/600 column with the free space split above and below — the same box, the same gap and the same two rules the verdict screens already share, written in the frame the same way (`content` at CENTER/CENTER filling the slot, rather than the fixed spacers `10` and `10b` use). Reusing it beats a fourth copy of three rules, so the resume screen imports it as it stands; what is wrong now is the **name**, which claims a verdict the screen does not have. Renaming it touches six screens that are already signed off, so it is logged rather than done — a call for the design owner.
- ~~**Knowie at size L over a `textBlock` variant L, centred in the free space.**~~ **Resolved — built at the third occurrence, as this entry said to.** `/explain/revisit-done` (20b Revisit complete) draws the same pair, so it is now `src/app/explain/MascotHeading.tsx`, used by `/explain/denied` (14), `/explain/resume` (05b) and `20b`. The gap is the prop, because it is the only thing the three frames disagree on — Space/400 on `14` and `20b`, Space/600 on `05b` — and `behindSheet` came with it, since `14` sits under the help sheet. `deniedScreen.css` is gone and `/explain/resume` no longer borrows `verdictBody.css`, which narrows that file back to the verdict screens it is named for. No story, and it stays with the screens rather than in `src/components`, for the same reason `SheetPanel` and `ActionStack` do.
- **No `textBlock` variant pairs a Headline S title with a Caption M Regular line** — `/explain/[term]/leave` (06b Leave session, confirm). The frame's sheet sets its question at Headline S over the reassurance at Caption M Regular, Space/050 apart. Variant **M** gets the title right and the caption one step larger, at Body S Regular on Space/200; variant **S** gets the gap right and drops the title to Body S Bold. The screen uses **M**, on the same reasoning as the alignment entry above — no prop, no fork — and because `textBlock.css` records that M's caption was *deliberately* moved off Caption M Regular: 12px under a heading was "a step below the smallest thing a student should have to read". The frame still draws the old pairing. If a second sheet wants this exact pair, the variant is the thing to add, not the screen's own type.
- **A third component with no `href` — `termRow`** — `/explain/summary` (17 Summary). Extends the `href` entry above. The row is a `<button>` that spreads button attributes and its own stories pass `onClick`, so `SummaryRows.tsx` is a client shim that pushes the route, exactly as `buttonIcon` and `recordButton` already need one. Three components now: if `href` is ever added to the library, add it to all three at once.
- **The claim headline has no `textBlock` variant.** `/explain/summary` (17 Summary) sets it at **Headline L**, centred — and `textBlock` skips that step: L is Headline XL and M is Headline S. So the claim is the screen's own `h1` with its type set from tokens, the same way `planScreen-subject` is. This is the second screen-level heading to fall outside the component (the plan's subject was the first); a third means `textBlock` wants the step, not the screen.
- **The XP total's outlined pill** — `/explain/summary` (17 Summary). A 2px `accent/brand/bold` ring at Radius/400 around `background/page`, holding a bolt and "+25 XP collected" at Headline S on Space/150. Nothing in Storybook is this: `statusTag` is the outcome word *inside* a row and `chips` is a key idea. Built in the screen. Its bolt is `iconSlot icon="zap"` — the frame draws a two-tone violet bolt that the repo's icon set does not carry, so the single-colour one stands in. `20 Home, revisit` and `21 Plan, section mastered` may want the same pill; if either does, it is a component.
- **The "Tap any term…" line is the fourth caption-grade line in the flow**, after the record button's helper, the text screen's helper and `CoveredIdeas`' label — which is exactly the count the entry above set as the trigger: "if a fourth caption-grade line turns up, the type step itself wants a home". `/explain/summary` (17 Summary) sets Caption M Regular on `text/secondary`, centred, in its own `<p>`. Not built here, because three of the four already exist in screens that are signed off and one of them is inside a component; the call is the design owner's.
- Per-family slot padding on `scaffold` — `/plan` and `/plan/in-progress` (02, 03), and due again on `/plan/to-revisit` (19), `/plan/mastered` (21) and both home screens. `scaffold.css` hard-codes one regime: `middleContent` at Space/200 top and `bottomContent` at Space/800 top and bottom, which is what all 25 session and verdict frames override their slots to. The plan frames use 0 top on `middleContent` and Space/400 on `bottomContent`; the home frames use 0 top and bottom on `bottomContent`. Three regimes, one component, and no prop for it — `Scaffold` takes `size`, the four slots and the three `show*` booleans, nothing else. Not built inline, because the padding belongs to the component and a page cancelling it with a negative margin breaks what `scaffold.css` says content inside must not do. **Needs a decision before it is built** — see the review of 02 and 03.
- ~~**The Home Indicator on `bottomNav`**~~ **Built.** Recorded first as a blocker on the grounds that 131 and 34 have no token and that the indicator was scaffold's job. **Both halves were wrong.** `scaffold` (3085:9242) draws no home indicator — it is a Panel Header holding a Status Bar instance, then the four slots — and all six mockups draw one at `bottomContent / Navbar / Home Indicator`, so it belongs to the bar. And 131 and 34 are iPhone geometry rather than design values, which `scaffold.css` had already settled for the 390x844 canvas: "neither 390 nor 844 needs a token, since the system has no dimension that large". The pill's 5 height, its full radius, its background/inverse fill and the Space/200 it sits off the bottom all come from tokens; only the two device numbers do not. **What is still open:** the file draws the pill 131 wide on the bottomNav component and the four plan mockups, and 119 wide on both home mockups and on the `Navbar` the component was cut from. Built at 131, because that is what the component says. One of the two wants correcting in Figma.
- **`scaffold` has no home indicator and design-system.md says it does.** Line 16 claimed scaffold "handles the status bar, the home indicator and the page background". The first and third are true. Corrected in design-system.md rather than left to trip up the next screen. Whether scaffold *should* own it — so that the 24 session screens get one too, which Figma does not draw for them — is a separate question nobody has asked yet.
- **`bottomNav` tells its active tab apart by colour alone** — `bottomNav`. accent/brand/bold against text/secondary, with no label, no shape and no underline, which is the one thing design-system.md's never list rules out. Figma draws it this way and the row has no space for a second signal at five tabs across 358. `aria-current="page"` carries it for a screen reader; nothing carries it in greyscale. Not fixed in code, because a second signal is a change to the component and therefore a decision.
- **A `bottomNav` tab is a 40x40 tap target**, under the 64 working floor design-system.md sets while 04-platform-constraints.md is missing. 40 is what Figma draws, and widening it in code would silently disagree with the file. Flagged rather than changed.
- **`navBar` is still on the gaps list and `bottomNav` does not close it.** design-system.md proposes `navBar` for "the home and plan mockups use Navbar, Navigation Button and Avatar from the unreachable library". `bottomNav` is the sprint's own bar and is built from `iconSlot`, because nothing local draws a tab; Navigation Button and Avatar stay unreachable. The four icons it needs — `myai-chat`, `search-md`, `target-04`, `trophy-02` — were exported from the Figma component into `public/icons` and added to `iconSlot`, and the avatar's image fill was exported to `public/images/avatar-knowie.png` because it is artwork, not a one-colour icon. The `navBar` proposal wants rewording or closing now that a bar exists.
- **`bottomNav` has no press state in Figma.** The component set is two variants on one axis and nothing else, so a tap has no acknowledgement in the file. Built in code from `interactive.pressed` and the press motion tokens the rest of the system uses, on the same grounds `button` gives for its sink. If that is wrong it is a variant on the component, not a screen's problem.
- **`chips` has no colour for a key-idea chip.** Every screen that draws the four key ideas overrides the chip's fill to `interactive/secondary` with an `interactive/onSecondary` label, in place of whatever its variant gives it. Three surfaces, and `chips` offers no variant that does it, so the override is written twice: `answerBlock.css` (`.knowieAnswerBlock-chips .knowieChips[data-active='False']`) for `kind=Answer`, and `coveredIdeas.css` (`.coveredIdeas-chips .knowieChips[data-color='Primary'][data-active]`) for `10 Got it` and `10b Got it, after a hint`. `chips.css` and the component set itself are untouched, so every other chip in the app keeps its variant's colour.

  The file overrides both states to the same paint, which is what makes this a variant and not a one-off: `answerBlock` and `10` set `active=False`, `10b` sets `active=True`, and all three render identically. On the two pass screens that costs the tick its colour — "You covered" above the group is what says they were covered now, which is enough, but `active` no longer shows on screen.

  This is the same shape as the snackbar chip override design-system.md already lists under "Where the file currently breaks these rules", and it wants the same fix: a `chips` colour for a chip that is a label rather than a control, decided by the owner. Until then the override stands in two stylesheets.
- **The home indicator makes scaffold's bottomContent padding a visible bug, not just a spacing one.** Extends the entry above. `scaffold.css` puts Space/800 under `bottomContent`, so on `/`, `/home/revisit` and the four plan routes the home indicator now floats ~32 off the bottom of the viewport instead of sitting on it. The Figma frames put the Navbar flush at the bottom — scaffold's `bottomContent` slot is 390x34 at the foot of the 844 canvas. A status-bar affordance that does not touch the screen edge reads as broken in a way a slightly roomy button never did. Still not fixed here, for the same reason as above: the padding belongs to `scaffold`, every screen depends on it, and a page cancelling it with a negative margin breaks what `scaffold.css` says content inside must not do. **This is now the strongest argument for taking that decision.**
